/**
 * Drizzle schema — multi-tenant AI Sales CRM.
 *
 * NOTE: This is a scaffold placeholder. The full schema (tenants, users,
 * channels, customers, conversations, messages, products, leads, orders,
 * knowledge, followups, usage_events, tenant_agreements, audit_logs, ...)
 * is built in the next step. See docs/02-plan.md and docs/03-requirements.md.
 *
 * Rule: every table except `tenants` MUST carry `tenant_id`, and every query
 * MUST filter by it (risk #8 — tenant isolation).
 */

export {};
